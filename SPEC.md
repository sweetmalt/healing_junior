# AI对话模块规格说明书

## 1. 功能概述

**模块名称**：OH卡AI对话助手
**核心功能**：在OH卡疗愈场景中，通过语音实时转文本、显示说话人区分、生成提示性问题，辅助疗愈师进行卡牌疗愈对话。
**目标用户**：疗愈师

---

## 2. 技术方案

### 2.1 语音识别

| 项目 | 方案 |
|------|------|
| **ASR服务商** | 火山引擎（豆包ASR） |
| **API类型** | WebSocket 双向流式 |
| **Endpoint** | `wss://openspeech.bytedance.com/api/v3/sauc/bigmodel_async` |
| **认证** | API Key: `10fcdf7f-d97d-4e4a-a0a1-6cbf628926d3` |

**请求头**：
```
X-Api-Key: 10fcdf7f-d97d-4e4a-a0a1-6cbf628926d3
X-Api-Resource-Id: volc.seedasr.sauc.duration
X-Api-Request-Id: <UUID>
X-Api-Sequence: -1
```

**二进制协议**：火山引擎使用特殊的二进制协议封装每个WebSocket包：
- Header (4字节) + PayloadSize (4字节) + Payload
- Header: `0x11` (version=1, header_size=4)
- Config消息: `0x10` (message_type=1, serialization=JSON)
- Audio消息: `0x20` (message_type=2, serialization=Binary)

**请求体**：
```json
{
  "user": {"uid": "user_<timestamp>"},
  "audio": {"format": "pcm", "rate": 16000, "bits": 16, "channel": 1, "codec": "raw"},
  "request": {
    "model_name": "bigmodel",
    "sequence": -1,
    "language": "zh-CN",
    "enable_partial": true,
    "enable_punc": true,
    "enable_nonstream": true,
    "enable_speaker_info": true,
    "ssd_version": "300"
  }
}
```

**音频格式**：PCM 16kHz 16bit 单声道

**说明**：
- `ssd_version: "300"` 启用词图分离，可获得更精确的说话人区分
- 多轮对话时，第一轮发送完整配置（`sequence: -1`），后续轮次只发送序列号（`sequence: 0,1,2...`）

### 2.2 本地录音

| 项目 | 方案 |
|------|------|
| **Flutter插件** | `record: ^6.0.0` |
| **采样率** | 16000 Hz |
| **编码** | PCM 16bit |

### 2.3 音频传输与心跳机制

为保证录音持续进行，采用以下策略：

| 机制 | 说明 |
|------|------|
| **服务端VAD** | 服务端配置 `mode: 'remote'`，由服务端判断语音 |
| **客户端直接发送** | 客户端不判断，直接发送所有音频数据（包括静音） |
| **心跳机制** | 每10秒发送640字节静音帧（20ms@16kHz），防止服务端静默超时断开 |

**为什么不用客户端VAD**：
- 客户端VAD判断会导致音频流不连续
- 服务端可能因收不到数据而超时断开
- 由服务端处理VAD更可靠

**心跳流程**：
```
录音流 ──────────────────────→ WebSocket发送
     │
     └────→ 每10秒发送静音帧 → 保持连接活跃
```

### 2.4 ASR结果处理

火山引擎ASR返回的数据中：
- `result.text` = **所有speaker的累积文本**（用于左侧完整记录）
- `result.utterances` = **每个speaker的累积文本数组**（用于分析）

处理策略：
- 左侧完整记录：直接显示 `text`（最新全文，覆盖显示）
- 分析用数据：从 `utterances` 获取每个speaker的累积文本，按标点分割成句子

---

## 3. 文件结构

**所有代码集中在 `lib/index.dart`**

```
lib/index.dart
├── VolcASRService          # 火山引擎ASR WebSocket封装（含心跳+自动重连）
├── ASRResult              # ASR识别结果数据类
├── SpeakerInfo            # 说话人信息（id + 标签）
├── AIDialogState          # 对话状态枚举
├── CozeHintService        # Coze提示问题生成服务
├── AIDialogCtrl           # AI对话控制器
├── AIDialogSheet          # AI对话滑动界面
└── _HintBubble            # 提示问题气泡组件
```

---

## 4. 界面结构（左右分区）

```
┌─────────────────────────────────────────────────────────┐
│  🧘 AI对话疗愈                              [连接中] [×] │
├─────────────────────────────┬───────────────────────────┤
│  📄 完整记录               │  💡 提示问题 (3条)         │
├─────────────────────────────┼───────────────────────────┤
│  【A说】这张卡让你想到什么？│                           │
│                             │  💡 树芽的"新生"让你想到  │
│  【B说】我觉得有点像...     │     什么未被浇灌的愿望？  │
│                             │                           │
│  【A说】嗯，继续说          │  💡 此刻你想如何"给这棵   │
│                             │     树"一点阳光？         │
│  【B说】最近工作压力很大    │                           │
├─────────────────────────────┴───────────────────────────┤
│                   [🎤 开始录音]                         │
└─────────────────────────────────────────────────────────┘
```

### 4.1 左侧：完整记录

- **标题**：`📄 完整记录`
- **内容**：所有已识别的语音文本，带说话人标记
- **标注**：说话人编码【A说】【B说】【C说】...
- **更新方式**：服务端返回新内容时覆盖显示
- **格式示例**：
  ```
  【A说】这张卡让你想到什么？
  【B说】我觉得有点像小时候的场景...
  ```

### 4.2 右侧：提示问题

- **标题**：`💡 提示问题`
- **内容**：由Coze智能体生成的引导性问题
- **显示顺序**：最新生成的显示在最下方（聊天应用惯例）
- **样式**：黄色背景 (#FFF8E1)，💡 图标，斜体文字
- **自动滚动**：新问题生成时自动滚动显示

### 4.3 提示问题生成

- **服务商**：Coze智能体（Bot ID: 7646075412124352548）
- **认证**：通过 `EmployeeCtrl.pay(0.1)` 获取 Coze Token
- **费用**：每次调用 0.1 元
- **输入参数**：
  - `conversation`：最近对话上下文（【说话人说】：内容）
  - `recentQuestions`：最近已生成的提示（避免重复）
- **输出格式**：`{"questions": ["问题1", "问题2"]}`
- **触发条件**：录音中时，每60秒生成一次
- **成本估算**：10分钟对话约10次调用，费用约1元

---

## 5. 核心逻辑

### 5.1 ASR数据处理

豆包ASR返回的数据结构：
- `result.text` = 所有speaker的累积文本（用于左侧完整记录）
- `result.utterances` = 每个speaker的累积文本数组

处理策略：
- **左侧完整记录**：直接显示 `text`（最新全文，覆盖显示）
- **分析用数据**：从 `utterances` 获取每个speaker的累积文本
- **去重**：比较本次 `text` 与上次，只有变化时才更新

### 5.2 说话人区分

服务端通过 `ssd_version: "300"` 启用词图分离，返回的每个 utterance 包含 `spkid`。

客户端处理：
- 首次出现的 spkid 分配标签（A, B, C, D...）
- 标签与 spkid 的映射存储在 `_speakerLabelMap`
- 识别结果按 utterances 顺序显示，带说话人标记

### 5.3 数据流程

```
ASR识别结果（服务端实时返回）
    │
    ▼
解析 utterances（每个speaker的累积文本）
    │
    ├─→ 更新每个speaker的累积文本（_speakerTexts）
    │
    ├─→ 拼接带说话人标记的完整文本
    │        示例：【A说】文本1\n【B说】文本2
    │
    └─→ 触发UI更新
             │
             ├─→ 左侧：覆盖显示完整文本
             │
             └─→ 首页talkList：显示最新文本片段
```

### 5.4 去重机制

- 比较本次 `text` 与上次 `_lastDisplayText`
- 只有变化时才更新显示，避免重复

---

## 6. 状态机

```
AIDialogState:
├── idle           # 未开启
├── connecting     # 连接中
├── recording      # 录音中
└── error          # 错误
```

---

## 7. 定时器

| 定时器 | 间隔 | 功能 |
|--------|------|------|
| `_hintTimer` | 60秒 | 调用Coze生成提示问题（仅录音中触发） |

---

## 8. 错误处理与重连

| 错误类型 | 处理方式 |
|----------|----------|
| 网络断开 | 提示用户检查网络 |
| ASR超时/静默断开 | 自动重连（最多3次） |
| 权限拒绝 | 提示用户开启麦克风权限 |

**自动重连机制**：
- `VolcASRService.attemptReconnect()` 实现重连逻辑
- 重连时递增 `sequence` 继续会话
- 重连成功后自动恢复心跳

---

## 9. 已知问题与解决

### 9.1 Protocol Version 错误
- **现象**：`unsupported protocol version 7`
- **误解**：最初以为是WebSocket版本问题
- **实际**：是火山引擎**二进制协议**的内部版本号
- **解决**：正确实现二进制头（`0x11` header + 4字节 size + payload）

### 9.2 中文乱码
- **现象**：识别结果中文字符显示为 `å¼å§`
- **原因**：`String.fromCharCodes` 无法正确解析UTF-8多字节字符
- **解决**：使用 `utf8.decode()` 正确解析

### 9.3 音频数据被拒绝
- **现象**：Config包成功，但音频包返回错误
- **原因**：音频数据也需要包装在二进制头中
- **解决**：Config用 `0x10` 头，Audio用 `0x20` 头

### 9.4 API调用成本优化（已废弃）
- ~~**问题**：实时语音识别成本较高，空闲时不应消耗~~
- ~~**解决**：实现VAD（语音活动检测），只在检测到声音时发送音频数据~~
- **注意**：此方案已被废弃，因会导致音频流不连续

### 9.5 静默超时断开
- **问题**：用户说话停顿时，服务端因收不到数据而超时断开
- **解决**：
  1. 移除客户端VAD判断，直接发送所有音频
  2. 每10秒发送心跳静音帧保持连接
  3. 断连后自动重连（最多3次）

---

## 10. 验收标准

1. ✅ 点击首页右下角FAB可弹出AI对话界面
2. ✅ 左右分区布局：左侧完整记录，右侧提示问题
3. ✅ 左侧实时显示识别内容，带说话人标记【A说】【B说】
4. ✅ 右侧仅显示Coze生成的提示问题（黄色样式）
5. ✅ 提示问题自动滚动显示最新一条
6. ✅ 录音停止时，提示问题生成也停止
7. ✅ 可下滑关闭对话界面
8. ✅ 录音/停止按钮正常工作
9. ✅ **持续录音**：用户说话停顿时不中断，心跳机制保持连接
10. ✅ **自动重连**：断连后自动重试（最多3次）
11. ✅ **停止录音立即断开**：用户点击停止后立即关闭WebSocket连接

---

## 11. 后续计划

### Phase 2：提示问题生成 ✅
- [x] 提示问题通过Coze智能体生成
- [x] 基于当前对话内容生成个性化提示
- [x] 录音状态受控（录音停止则停止生成）
- [x] 每60秒自动生成一次

### Phase 3：会话报告生成
- [ ] 会话结束后生成完整报告（具体方案待确认）
- [ ] 报告包含：对话摘要、关键洞察、疗愈建议
- [ ] 报告展示/导出功能

### Phase 4：会话持久化
- [ ] 会话数据保存到本地/服务器
- [ ] 历史会话查询功能
